[build]
target = "index.html"
dist = "dist"

[watch]
# Ignore auto generated files
ignore = [ "src/vfs.json" ]
watch = [ "../data", "src" ]

[[hooks]]
stage = "pre_build" 
command = "cargo"
command_arguments = ['xtask', "metadata-generate"]
